# Dependencies of the automation/ layer beyond upstream's pyproject.toml
# (which already provides pandas and requests).
#
#   pip install -r automation/requirements.txt
#
# pyyaml and python-dotenv are imported directly by this layer; today they
# arrive transitively via the upstream LLM stack, but are declared here so
# the layer survives upstream dependency changes.
#
# itsdangerous signs report-link tokens (automation/tokens.py) — the
# de facto standard for exactly this (Flask's own session/reset-link
# library), avoids hand-rolling HMAC+timestamp parsing.
#
# slowapi (+ its own dependency, limits) rate-limits the public report
# endpoint (web/server.py) and, used directly, the bot's per-user ticker
# throttle (bot.py) — one shared, tested rate-limit implementation
# instead of two hand-rolled ones.

streamlit>=1.58
bleach>=6.1.0
fastapi>=0.115.0
markdown>=3.7
pyyaml>=6.0
python-dotenv>=1.0
itsdangerous>=2.1.0
slowapi>=0.1.9
limits>=3.0
uvicorn[standard]>=0.30.0
yfinance>=0.2.0
langgraph>=0.1.0
stockstats>=0.4.1
langgraph-checkpoint-sqlite
langchain_openai>=0.1.0
